config ZMK_BEHAVIOR_BT_LAYER
    bool "Enable BT profile-layer behavior"
    default y
    depends on ZMK_BLE
    help
      Enable a custom behavior that selects a Bluetooth profile and toggles
      keymap layers to stay in sync.
