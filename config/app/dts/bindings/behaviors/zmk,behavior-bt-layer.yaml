# SPDX-License-Identifier: MIT

description: Bluetooth profile select behavior with layer updates

compatible: "zmk,behavior-bt-layer"

include: behavior-metadata.yaml

properties:
  label:
    type: string
    required: false
  "#binding-cells":
    type: int
    required: true
    const: 1

binding-cells:
  - profile
